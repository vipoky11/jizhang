# 配置说明

## ✅ 数据库账号已创建成功！

已成功创建以下数据库账号：

- **用户名**: `caiwuguanli`
- **密码**: `caiwuguanli123`
- **数据库**: `dish_accounting`
- **权限**: 对 `dish_accounting` 数据库拥有所有权限

## 📝 配置 .env 文件

请创建 `.env` 文件（如果还没有），内容如下：

```env
DB_HOST=localhost
DB_USER=caiwuguanli
DB_PASSWORD=caiwuguanli123
DB_NAME=dish_accounting
DB_PORT=3306
PORT=5000
```

### 创建 .env 文件的方法：

**方法 1: 使用命令行**
```bash
cat > .env << 'EOF'
DB_HOST=localhost
DB_USER=caiwuguanli
DB_PASSWORD=caiwuguanli123
DB_NAME=dish_accounting
DB_PORT=3306
PORT=5000
EOF
```

**方法 2: 复制示例文件并编辑**
```bash
cp env.example .env
# 然后编辑 .env 文件
```

**方法 3: 手动创建**
在项目根目录创建 `.env` 文件，复制上面的内容。

## 🚀 启动项目

配置完成后，可以启动项目：

```bash
# 同时启动前后端
npm run dev

# 或分别启动
npm run server  # 后端
npm run client  # 前端
```

## ✅ 验证配置

可以使用以下命令测试数据库连接：

```bash
npm run test-db
```

## 📋 已完成的设置

- ✅ 数据库 `dish_accounting` 已创建
- ✅ 用户 `caiwuguanli` 已创建
- ✅ 用户权限已授予
- ✅ 表结构已初始化

现在你可以开始使用系统了！

